export const verificationStatuses = {
  required: 0,
  loading: 1,
  success: 2,
  fail: 3,
};

export const notificationStatuses = {
  idle: 0,
  success: 1,
  error: 2,
  changed: 3,
};

export const storageTypes = {
  local: 'local',
  s3: 's3',
};
